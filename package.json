{
  "name": "orquestaflow",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n back,front -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "test": "npm --prefix backend test && npm --prefix frontend test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
